import { styled } from "../../../stitches.config";

const EventsWrapper = styled('div', {});

export const Events = () => {
    return (
        <EventsWrapper>
            Events
        </EventsWrapper>
    )
}

export default Events;