import { styled } from "../../../stitches.config";

const AdministratorsWrapper = styled('div', {});

export const Administrators = () => {
    return (
        <AdministratorsWrapper>Admins</AdministratorsWrapper>
    )
}

export default Administrators;