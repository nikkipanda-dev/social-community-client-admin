import { styled } from "../../../stitches.config";

const AnchorWrapper = styled('a', {});

export const Anchor = () => {
    return (
        <AnchorWrapper>
            Anchor
        </AnchorWrapper>
    )
}

export default Anchor;