import { styled } from "../../../stitches.config";

const MembersWrapper = styled('div', {});

export const Members = () => {
    return (
        <MembersWrapper>
            Members section
        </MembersWrapper>
    )
}
